# Wiki Log

This file is the active chronological operational log for this wiki.

Add one entry per meaningful operation (ingest, revise, reorganize, synthesize, maintenance).

## Entry format

Use:

`## [YYYY-MM-DD] <operation> | <subject>`

Where:

- `operation` is a short verb like `ingest`, `revise`, `synthesize`, `reorganize`, `maintain`
- `subject` is usually a source slug (e.g. `paper-react-en`) or a note name

## [2026-04-25] scaffold | initialize repo structure

Created the initial `raw/`, `agent/`, and `wiki/` scaffold, plus public onboarding docs and templates.
